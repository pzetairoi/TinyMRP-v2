========================================================================
POST-HARDENING PLAN — the route to a professional finish
========================================================================
Rewritten clean 2026-08-07. Branch: main.

HOW TO USE THIS FILE
  This is the execution document: what is done, what is left, and who can
  actually close each item. hardeningplan.txt is now the HISTORICAL record -
  read it for WHY things were built as they were and for the incident
  write-ups, not to decide what to work on next.

  Infrastructure is referred to by ALIAS. The real addresses and domains live
  in deploy/fleet.local.md, which is gitignored. This repository is public.


========================================================================
1. PROGRESS BOARD
========================================================================

  ENGINEERING                                       STATE
  ------------------------------------------------  --------------------
  E1  Error boundary + real 404                     DONE
  E2  Silent exception handlers                     28 of 184 + 2 REAL BUGS
  E3  /api/ready wired into every healthcheck       DONE
  E4  Redis in the guided per-instance compose      DONE
  E5  Per-endpoint rate limits, enforced            DONE
  E6  Page tests for the remaining pages            1 of 5
  E7  Backup restore drill                          DONE - PASSED TWICE
  E7b Backup checksums                              DONE
  E8  Accessibility baseline                        PARTIAL (structure)
  E9  Concurrent note edits                         DONE
  E10 Playwright + axe                              NOT STARTED
  P1  Resource and speed optimisation                NOT STARTED - see 4e
      *** The owner considers the plan UNFINISHED until this is done. ***
  B1  BOM child links sent the wrong revision       DONE
  B2  bom_flat storage scans                        DONE
  B3  Parts inventory latency                       FIXED, UNMEASURED
  B4  bom_flat N+1 on the DATABASE                  DONE - batched
  B5  where-used link gives "not found"             DONE - case-sensitivity
  B6  Imports failed on EVERY hardened deploy       DONE - was critical
  B7  Failed import did not say what landed         DONE
  E11 Remove dead files, configs, templates         DONE (first pass)
  E12 Group SolidWorks files on Tools + help        DONE

  OWNER                                             STATE
  ------------------------------------------------  --------------------
  O1  CSP browser check      ~30 min, closes a P1   WAITING ON YOU
  O2  Mongo authentication across the fleet         1 of 4 instances
  O3  Guided install from scratch                   BLOCKED: needs a TTY
  O4  Product position                              DECIDED 2026-08-07
  O5  Legal/commercial package                      DRAFTING
  O6  Trivy gate                                    DECIDED: keep as is
  O7  Redis for existing instances                  3 of 4 instances
  L1  Strict security mode across the fleet         3 of 4 - ADD-IN VERIFIED

  GATES: 764 backend, 322 frontend, ruff/mypy/bandit clean, build OK.

  HELP SCREENSHOTS: five placeholder images now appear in the help, each one
  stating what to capture and which file to save over. Replace them one at a
  time - app/static/help/img/<name>.png. Nothing else needs changing; the help
  already references them.
  FOUND WHILE DOING IT: drawing markups have NO help section at all, only a
  troubleshooting entry. A whole feature is undocumented.

  WAITING ON A MEASUREMENT: B3's final fix is deployed to test but has not been
  timed. Load /ui/parts with LOG_LEVEL=DEBUG on that instance to confirm it.


========================================================================
2. FLEET — four instances on two servers
========================================================================
  Aliases only. Real addresses in deploy/fleet.local.md (gitignored).

  ALL THREE reachable instances verified 2026-08-07 on faf9d6c9f3cf:
  healthy, strict mode, redis running, /api/ready ok. mecs deliberately
  untouched. Every public endpoint on both shared Caddies still serves,
  including the Nextclouds and Vikunja.

  SERVER-A
    main    52 parts,   23 MB   PRODUCTION
            latest | Mongo AUTHENTICATED | redis | STRICT | ready: ok
            shares its Caddy with a Nextcloud and a Vikunja - both verified up

  SERVER-B                                          15 GB free - watch this
    demo    12 parts,  5.2 MB   disposable
            latest | redis | STRICT | ready: ok (mongodb_unauthenticated)
    test  1789 parts,  6.8 GB   *** NOT A SANDBOX - see below ***
            latest | redis | STRICT | ready: ok (mongodb_unauthenticated)
    mecs  1479 parts,  2.8 GB   PRODUCTION - OWNER SAID DO NOT TOUCH
            pre-hardening image, compat, no redis - untouched and verified so
    shares its Caddy with two Nextclouds - both verified up

  *** The instance named "test" holds MORE parts and more than twice the data
  of the mecs production instance. Its name is misleading: deleting or
  reinstalling it would destroy real work. The only genuinely disposable
  instance on either server is demo. ***

  demo, test and main run STRICT mode. Only mecs remains on compat (see L1).

  Access: key ~/.ssh/tinymrp_deploy, tagged claude-tinymrp-deploy-20260807 in
  each server's authorized_keys. Remove that line to revoke; nothing else
  needs rotating. The password used to bootstrap it appeared in a chat
  transcript and should be changed.


========================================================================
3. WHAT IS LEFT — owner
========================================================================

  O1. CSP browser check                    ~30 min, closes a P1
      All 34 inline handlers are delegated and all 18 inline scripts carry a
      nonce. Only a human with a browser can confirm nothing broke.
        1. start with TINYMRP_CSP_REPORT_ONLY_STRICT=true
        2. exercise the admin UI - jobs, orders, fields, roles, add-in
        3. watch the console for CSP violations
        4. if clean, set TINYMRP_CSP_ALLOW_INLINE=false and repeat
      Best value per minute of anything left in this document.

  O2. Mongo authentication — 3 instances remain
      DONE on main via deploy/scripts/enable-mongo-auth.sh. Verified after:
      anonymous access refused, and the application credential denied the
      admin database.
      Remaining: demo, test, mecs. Same script, one short maintenance window
      each. It backs up first and refuses to continue if the backup fails.

  O3. Guided install from scratch — BLOCKED, needs your terminal
      Not a defect any more, just a limit: without a TTY the installer cannot
      display the generated admin password or accept one you type, and that
      password is printed exactly once.
      Run it yourself, on SERVER-B, where DNS already resolves correctly:

        cd /opt/TinyMRP-v2 && sudo git pull
        sudo ./deploy/scripts/create-instance.sh <name> <domain> \
            --admin-email you@example.com

      Add --admin-password '<choice>' to set it, or let it generate one and
      COPY IT when printed. Use a NEW instance name - never an existing one.
      What to watch, since no guided install has ever been run:
        - the Caddy route installs, validates and reloads WITHOUT disturbing
          the other sites on that shared proxy
        - the app reaches healthy against /api/ready
        - a redis container appears beside app and mongo
        - the instance .env carries MONGO_APP_USER and authSource in MONGO_URI

  O4. Product position — DECIDED 2026-08-07 by the owner
      TRACK A. TinyMRP is NOT a complete MRP and must never be marketed as one.

      WHAT IT IS, in the owner's words: a way for small engineering offices to
      pass information easily to purchasing and to the workshop. It handles
      jobs and purchase listings, parts and revisions, BOMs, engineering files,
      drawing review and controlled document packs, with SolidWorks
      integration.

      WHAT IT IS NOT, and must not be claimed: there is NO inventory. No stock
      ledger, no warehouses or bins, no receipts or material issues, no MRP
      explosion or demand netting, no lead times or reorder policies, no
      routings, work centres or capacity planning, no costing, no lot or serial
      traceability, no cycle counting.

      The word "MRP" in the product name is therefore a liability in marketing
      copy unless the scope is stated plainly beside it.

  O5. Commercial/legal package — drafting, owner reviews with counsel
      LICENCE DECIDED 2026-08-07: The Unlicense STAYS. The owner was told
      plainly that this is a public-domain dedication - anyone may copy,
      modify, sell and compete using TinyMRP, owing nothing - and confirmed
      that is intended.

      *** THE CONSEQUENCE THAT SHAPES EVERYTHING ELSE HERE: you cannot impose
      use restrictions on software you have dedicated to the public domain. So
      there can be no EULA in the usual sense - no "you may not redistribute",
      no per-seat limits, no field-of-use terms. Any such clause would
      contradict the licence and be unenforceable.
      What CAN be agreed is everything that is not the software: hosting,
      support, response times, data handling, backups, confidentiality of the
      customer's own data. The document must therefore be a SERVICE AND
      SUPPORT AGREEMENT, not a software licence. Drafting it as a EULA would
      produce something that reads binding and is not. ***

      Drafts to produce, all marked clearly as pending legal review:
        - service and support agreement (not a EULA - see above)
        - privacy policy and a data-processing addendum
        - retention and deletion policy
        - support scope and response targets
        - vulnerability disclosure contact and process
        - third-party software notices
        - accessibility statement

  O6. Trivy — DECIDED 2026-08-07: leave as is.
      It blocks a release on a HIGH or CRITICAL image vulnerability THAT HAS A
      FIX. Unfixable ones are ignored, so it only fires on something
      actionable. (An earlier audit note claiming it did not block was wrong:
      the continue-on-error only lets the SBOM upload run before the
      enforcement step fails the job.)

  O7. Redis for existing instances — 1 remains
      DONE on main, demo and test. Remaining: mecs.
      Per instance: add RATE_LIMIT_STORAGE_URI=redis://redis:6379/0 to its
      .env, then bring the FULL stack up once so redis starts. It cannot be
      done through update-instance.sh - see section 6.

  L1. Strict security mode — 3 of 4 done, and the risk is now retired
      *** THE ADD-IN WAS TESTED AGAINST STRICT MODE AND WORKS. That was the
      last big unknown in this whole effort: strict rejects bearer
      substitution on browser APIs and enforces an origin/referer CSRF check,
      and only the owner could tell whether the SolidWorks integration
      survived it. It does. ***
      Rolled out to demo and test on 2026-08-07, then to main once the add-in
      was confirmed. Each was applied with automatic revert if the app failed
      to become healthy; it was never needed. All three report security_mode
      "strict" and all three still serve.
      Phase 2 authentication hardening is therefore ACTIVE IN PRODUCTION for
      the first time - previously it existed only in the test suite.
      REMAINING: mecs, which the owner has kept hands-off. One environment
      variable and a restart, whenever they choose.

========================================================================
4. WHAT IS LEFT — engineering
========================================================================

  E2. Silent exception handlers — 162 of 184 remain
      *** THE TRIAGE FOUND A REAL SECURITY BUG, not just a debuggability one.
      Bulk user deletion FAILED OPEN. The guard protecting administrators
      swallowed its own exception and fell through to u.delete(), so a broken
      role reference or a momentary database error deleted the very
      administrator it exists to protect - and the same for the guard stopping
      you deleting your own account. Deletion is irreversible, so both now skip
      the user and say so. Pinned by a test that also fails if any bare pass is
      reintroduced anywhere in that loop. ***
      Also converted on this pass:
        runtime_secrets - a failed chmod 0600 on a SECRETS file was silent, so
          a world-readable secrets file would never have been noticed
        extra_files - the ROLLBACK itself failed silently, leaving the database
          holding a record the filesystem no longer matched, with nothing
          saying which record to reconcile
        a failed user deletion is now counted and surfaced: references are
          cleaned up BEFORE the delete, so a failure leaves damage behind

  E2-old. Earlier pass — 170 of 184 remained
      Triage, not a rewrite: suppressing a thumbnail render inside a loop is
      legitimate; suppressing a write is how a defect becomes undebuggable.
      DONE: the 14 that can hide data or security loss - the import journal
      (which was LYING about the stage reached when a write failed), file
      deletion, an authorization denial with no audit trail, public share
      creation, and five review notifications.
      LEFT ALONE on purpose: audit.log_action's own nested guards. An audit
      failure must never break the operation it is recording.
      REMAINING, concentrated in docpacks.py (36), admin.py (20), parts.py
      (20) and __init__.py (19). Mostly best-effort UI and rendering paths,
      but they have not been read one by one.

  E6. Page tests — 4 of 5 remain
      DONE: UploadPackPage, chosen first because it is where an operator
      decides whether to import against live data. Pins fail-closed capability
      gating: a permission lookup that FAILS must never leave a destructive
      action reachable.
      REMAINING: PartsPage, PartDetailPage, AdminFieldsPage, AdminAddinPage.
      GOTCHA: mockApi treats an entry with no `body` key as the body itself,
      so { status: 500 } silently returns 200. Error paths need
      { status: 500, body: {...} }.

  E8. Accessibility — structure done, behaviour not
      DONE: skip link (first focusable element, visible only on focus), a
      <main id="main-content"> landmark where there was NO landmark at all,
      and prefers-reduced-motion across the tree. Labelling was already
      largely in place (63 htmlFor, 48 aria-label).
      REMAINING: keyboard-only pass over real workflows, modal focus
      management, colour contrast, accessible names on icon-only controls,
      more live regions (only 2 today). All need a browser - see E10.

  E10. Playwright + axe — NOT STARTED, the largest single item
      Needs CI to boot the app plus Mongo. Also the only way to test the 3D
      viewer and canvas markup, which jsdom cannot run.

  E7 residue. Backups are unencrypted and on-host.
      Checksums are written and verified. The owner chose to skip encryption.
      Off-host copying remains an owner decision. NOTE: SERVER-B has only
      15 GB free against a 6.8 GB instance, so a full backup there is tight,
      and a careless one could fill a disk shared with production.

  DELIBERATELY NOT DOING, and why
    - Mongo transactions for imports: needs a replica set. The journal is the
      designed alternative and it works.
    - Repo-wide Black reformat: 162 files of churn would bury every real diff.
    - UP006/UP045 annotation rewrites (~1100 sites), SIM105 (103 sites):
      enormous churn, no runtime benefit, on code running in production.
    - Admin UI over the import journal: the data is reachable over the API,
      which was the blocking part. A page is polish.
    - Breaking up docpacks.py (3336 lines) and friends: real debt, but
      high-risk surgery with no test safety net under it yet. Wants E6 first.
    - Optimistic concurrency on PARTS: parts are not browser-editable at all.
      Only notes were, and that is done (E9).


========================================================================
4b. BUGS REPORTED FROM THE LIVE TEST INSTANCE
========================================================================

  B1. BOM child links sent the parent's revision — FIXED 2026-08-07
      Reported as "part details ok but buggy with the links". The address bar
      was correct and every request still failed.
      React does not unmount the detail page when only a route param changes,
      so the previously loaded part stayed in state and supplied ITS revision
      until the new fetch landed. Clicking a child whose revision is blank,
      from a parent at revision "1", sent part_detail, files_overview, shares,
      images and docpack options all out with rev=1 - a page of 404s.
      A loaded part now supplies the revision only while it IS the part in the
      URL. Extracted to src/lib/partIdentity.ts with 5 tests, matched
      case-insensitively because the backend matches part numbers that way.
      Very likely the same root cause as the older report that a child's BOM
      stayed visible while its parent loaded.

  B2. bom_flat stormed the storage — FIXED 2026-08-07
      Clicking a large child timed out. bom_flat was the unoptimised sibling of
      bom_tree: when bom_tree went from 35s to 77ms it gained batched lookups,
      and bom_flat kept calling preview_png_urls_for once per unique part at
      ~65 ms each. Now one link walk collects every distinct part and all
      thumbnails resolve in a single batch.

  B3. Parts inventory latency — THREE PASSES, THE LAST ONE UNMEASURED
      Measured with the endpoint's own instrumentation, never guessed.

        BEFORE   config=46 filters=0.2 query=71 thumbs=524 rows=64  total=705ms
        PASS 1   thumbs=370-476                                     total=529-678ms
        PASS 3   deployed to test, NOT YET TIMED

      RULED OUT, so nobody re-investigates: parts_lazy already paginates
      (25 rows), preview_png_urls_map already batches at the DATABASE level,
      the created_at sort is indexed, and /api/field-config is 55 KB of SIZE
      not latency (its computation is the config=~46 ms bucket - real, but not
      the problem).

      Pass 1 - a request-scoped directory listing cache. Only ~25%, and the
      reason is worth keeping: I assumed each part's files sat in their own
      directory. The storage is FLAT - 33 directories for 10990 files, with
      thumbs/png holding 2457 alone - so the one scandir reads 2457 entries and
      eats most of the saving.

      Pass 2 - _validate_candidate re-resolved the storage ROOT for every
      candidate, the same path dozens of times per request. Memoised.

      Pass 3, the real fix - stop asking the filesystem at all.
      thumb_mtime already existed on PartFile and is written the moment a
      thumbnail is generated, so availability is answered from the record.
      Records without it still fall back to disk, so older data is unchanged.
      SAFE FOR TWO REASONS, both stated because "trust the DB" is normally the
      wrong instinct in file_security.py:
        - a wrong YES cannot grant access. It only decides which URL goes on a
          row; serving still resolves with must_exist=True, so a deleted file
          is a 404, never a bypass.
        - a 404 is not a broken page. ThumbImg walks its candidates and settles
          on the branding logo, so a stale record degrades to a placeholder.

      NEXT: load /ui/parts on test with LOG_LEVEL=DEBUG and compare. If thumbs
      is still the dominant bucket, the remaining cost is Path.resolve() per
      candidate and the option not yet tried is resolving relative to an
      already-resolved root.

      CONSTRAINT for whoever continues: this is file_security.py, the
      path-traversal guard. Speed must never come from weakening containment.
      tests/test_file_security_listing_cache.py pins that boundary - a file
      that genuinely exists outside the root is still refused, traversal is
      still refused, and the cache is proved off outside a request context.

========================================================================
4c. B4 — bom_flat is still O(N) DATABASE queries  *** CAUSES 502s ***
========================================================================
  Reported: a part detail returns 502, and the flat BOM "takes forever".
  Both are the same thing. gunicorn runs --timeout 120 with 2 workers, so a
  request that outlives that is killed and the proxy answers 502.

  MEASURED on the reported part: 7 levels deep, 183 link rows, 163 DISTINCT
  parts.

  The thumbnail batching (B2) fixed the FILESYSTEM side. The database side is
  untouched: row_for() runs per distinct part and each one does
    - scoped_parts.filter(...).first()      one query per part
    - _coverage_groups(pn, rev)             another lookup per part
    - resolve_part_field_values / filter_response_fields
  plus _bom_is_fully_authorised walking the whole subtree first, and
  authorised_part_pairs called again per node during the descent.

  So ~163 parts becomes several hundred round trips. That is the remaining
  cost, and it grows with assembly size - which is why small instances never
  showed it.

  THE FIX, same shape as every other one here: collect the distinct pairs
  first (the walk already exists - _flat_subtree_pairs), then resolve parts,
  coverage and field values in BATCHES, and drop the redundant per-node
  authorisation now that the subtree is proved once up front.
  NOT started: this is a real refactor of bom_flat's traversal, not a patch,
  and it deserves a full pass rather than the tail of one.

  INTERIM RELIEF if it bites before then: raising gunicorn --timeout turns a
  502 into a slow page, which is less alarming but not a fix.

========================================================================
4c-b. B5 — a where-used link answers "not found"
========================================================================
  Reported: clicking where-used on a parent lands on /ui/part/<pn>?rev= and
  shows "not found".

  RULED OUT already, so start from here rather than repeating it:
    - the part EXISTS: one row, revision blank, and the URL carries a blank
      revision too, so the data is not the problem
    - it is NOT the stale-revision bug (B1). That fix IS deployed: the bundle
      the browser loads, index-fUqBhSZ0.js, was built in commit 15e42b0 which
      is the fix itself
    - the links are constructed correctly - /ui/part/${pn}?rev=${rev} with both
      parts encoded

  THE LEAD: PartDetailPage builds part links two different ways. Lines 829/834
  use <Link to=...>, which routes inside the SPA. Lines 772/791 use a raw
  <a href=...>, which is a FULL PAGE LOAD and therefore hits Flask, not the
  browser router. A "not found" from that path comes from the SERVER route for
  /ui/part/<pn>, which is a different code path from the /api/part_detail
  lookups already investigated.
  Check the server-side /ui/part/<pn> handler first, and while there, decide
  whether those two <a href> links should be <Link> like their neighbours - a
  full reload of a heavy SPA is also why those clicks feel slow.

  NOTE: this may have been failing all along and only became VISIBLE when the
  catch-all route started rendering a real 404 instead of quietly showing the
  parts inventory (E1). Worth confirming, because it changes whether this is a
  regression or an old bug finally surfacing.

========================================================================
4c-c. B6 — imports were broken on EVERY hardened deployment  [FIXED]
========================================================================
  The most serious defect found in this whole effort, and it was invisible
  until the app ran on a real hardened host.

  Reported twice with different packs: "cross-store file commit failed at
  <part> revision (blank)". Every import that wrote a file failed.

  CAUSE: _commit_files ends with os.replace, which is atomic but cannot cross
  a filesystem boundary - it raises EXDEV. Staging went to the system temp
  directory, and the hardened container runs a read-only root with a tmpfs
  /tmp, so temp is a DIFFERENT DEVICE from the bind-mounted deliverables
  volume. Measured on the instance: /tmp device 162, /data/deliverables 2065.
  The container hardening and the import path were each correct alone and
  incompatible together. No test could have caught it - both halves passed.

  FIX: stage inside the deliverables root, which _stage_files already
  received. The commit stays ATOMIC rather than degrading to a copy; a copy
  failing halfway would leave a truncated file where a whole one used to be.

  WHAT WENT RIGHT: the journal behaved exactly as designed. Both failed runs
  rolled back cleanly, reversed every created part, and needed no manual
  follow-up. The data was never at risk - only the operator's ability to see
  that, which is B7.

========================================================================
4c-d. B7 — a failed import now says whether anything landed  [FIXED]
========================================================================
  The operator saw an exception and could not tell a clean abort from a
  half-applied import. That is the only question that matters afterwards: it
  decides whether you retry or reconcile by hand.
  The journal already knew. The failure response now carries it in plain
  words - nothing kept and how many changes were reversed, or that it needs
  checking by hand - plus the operation id.

========================================================================
4c-e. B5 — where-used "not found": TWO authorisation paths disagree
========================================================================
  The 404 is NOT missing data. /ui/part/<pn> in app/views/ui.py ends with
      if not authorised_get(scoped, current_user, pn, ...): abort(404)
  so it is an authorisation denial presented as "not found" - correct on its
  own (do not leak existence), but it contradicts the page that linked there.

  THE REAL PROBLEM: where-used scopes with scope_queryset and
  authorised_part_pairs, while the detail route uses authorised_get. Two
  mechanisms, two answers for the same part. One of them is wrong:
    - if where-used is more permissive, it LEAKS parts the user may not see
    - if the detail route is stricter, it is a usability bug
  Deciding which needs the owner's intent for that role, so it is written down
  rather than guessed at. Reconciling them on one mechanism is the fix.

========================================================================
4d. E11 / E12 — repository cleanup and the Tools page
========================================================================

  E11. Remove dead and misleading files
      Baseline measured 2026-08-07 so the reduction can be shown:
        .git objects        71 MB   (size-pack 69 MiB)
        tracked tree zip    6.7 MB
        app/static          7.2 MB   <- largest tracked folder
        solidworks-addin    1.2 MB
      Known candidates found so far:
        - app/templates/security/login_user.html  ORPHAN, referenced nowhere
        - app/static/misc/TinyMRP.bmp             537 KB, no reference found
        - app/static/misc/test.3mf                a test artefact, shipped
        - app/static/tools/                       the tools page lists this
          directory and it does not exist, so _static_tools() always returns
          empty - dead code path and a misleading empty state
        - handycommands.txt, serverdeploymentguide.txt at repo root: personal
          notes shipped to every clone
      Method: prove each is unreferenced before deleting, delete in ONE commit
      per category so a mistake is trivially revertible, and re-measure.

  E12. Group the SolidWorks files on the Tools page, and update the help
      app/static/misc holds 17 entries. Only FOUR are reachable from the Tools
      page, hardcoded one by one in tools/index.html:
        tinymrp_part_customtab.prtprp, tinymrp_assy_customtab.asmprp,
        excelcompile.xlsx, plus the macro via main.download_macro
      Not surfaced anywhere, but shipped:
        TinyMRP.swp (the macro itself), TinyMRP_BOM_TEMPLATE.sldbomtbt,
        "BOM FOR PROCESSING.sldbomtbt", "custom property writer.xlsm",
        excel_property_writer.bas, TinyMRP_conf.xlsm, REFMODELS/, SOLIDSETUP/
      Proposed grouping on the Tools page:
        Add-in          installer, macro
        SolidWorks setup  custom tab properties, reference models, setup files
        BOM templates   the two .sldbomtbt tables
        Excel helpers   compile template, property writer, conf workbook
      The listing should be DRIVEN BY THE FILESYSTEM with a declared group per
      pattern, not another hand-maintained list of links - the current one
      already drifted out of date, which is how eight files became invisible.
      Then update the help so each group says what the file is FOR; several of
      these are unusable without that.

========================================================================
4e. P1 — RESOURCE AND SPEED OPTIMISATION  *** blocks completion ***
========================================================================
  Raised by the owner 2026-08-08, and the framing is theirs and correct:

    "the most important thing in this project - browsing part numbers and
     their trees fluidly - we have ruined with so much security and checking"

  That is a fair verdict. Low resource use was an original goal of TinyMRP and
  the hardening traded it away without measuring what it cost.

  EVIDENCE ALREADY GATHERED (all measured on the live test instance, never
  guessed - keep it, it is the baseline):

    Mongo sat at ~51% CPU with nobody using the system.
    1,269,017 find queries in 937 minutes = ~22 per second, sustained, idle.
    24,903 connections created against 17 current - enormous churn.
    _bom_is_fully_authorised: 1801 ms to walk 1347 parts, on EVERY flat-BOM
      request, for an answer that could not have been "no".
    /api/notifications polled 2405 times in 6 hours from a 60s interval in
      security/base.html - noisy, though ~7/min is not itself saturating.
    2 gunicorn workers x 4 threads = 8 concurrent slots for a page that fires
      about a dozen requests.

  ALREADY FIXED, and the rest of the method should follow their shape:
    - workers sized from nproc, (2 x cores) + 1, floored 4 capped 12
    - the subtree authorisation walk skipped when the scope filters nothing
    - bom_flat: parts batched into one query, per-node re-authorisation removed
    - thumbnails answered from the database instead of the filesystem

  *** THE OWNER NAVIGATES WITH bom_tree, NOT bom_flat. Most tuning so far went
  into bom_flat. bom_tree is the hot path and has NOT been profiled. Start
  there. ***

  METHOD - do it in this order, and do not skip step 1:

    1. MEASURE BEFORE CHANGING. The instrumentation already in parts_lazy is
       the model: per-phase timings behind LOG_LEVEL=DEBUG. Add the same to
       bom_tree and part_detail. Never optimise a phase you have not timed -
       two attempts in this project targeted the wrong thing because the
       assumption sounded right.

    2. FIND THE QUERY COUNT PER REQUEST. 22 finds/second while idle means
       something loops. Use the Mongo profiler (setProfilingLevel(2)) over a
       short window during real navigation and group by namespace and shape.
       An N+1 hides as a fast query repeated hundreds of times - every
       individual query here already runs in 1 ms and returns 3 documents.

    3. FIX BY BATCHING, NOT BY CACHING. Every win so far came from replacing
       N round trips with one. Caching would have hidden the same work behind
       a hit rate and rotted the moment data changed.

    4. DELETE CHECKS THAT CANNOT FAIL, rather than making them faster. The
       1801 ms walk was correct code doing pointless work. Look for others:
       re-authorising inside a loop whose scope was already proved, re-reading
       config per row, re-resolving the same path per file.

    5. RE-MEASURE AND RECORD BOTH NUMBERS. A change without a before and after
       is a guess. Put both in the commit message.

  KNOWN LEADS, NOT YET INVESTIGATED:
    - What produces ~22 finds/second on an idle instance. This is the single
      biggest open question and it is pure waste.
    - Connection churn: 24,903 created for 17 concurrent suggests the Mongo
      pool is not being reused across requests.
    - bom_tree per-request cost: never profiled.
    - The 60s notification poll fires on every page in security/base.html; a
      full page navigation restarts it, and the BOM links are full page loads.
    - resolve_part_field_values and filter_response_fields run per row and have
      never been timed.

  EXIT CRITERIA:
    - Browsing a large assembly stays responsive with no hang and no recovery
      period, verified on the test instance with its 1789 parts.
    - Mongo CPU near zero when nobody is using the system.
    - Query count per page load recorded, and justified.
    - Before/after numbers in the plan for each change.

========================================================================
5. THE SHORTEST FINISH
========================================================================

  Two tracks, independent of each other.

  OWNER                              ENGINEERING
  ---------------------------------  ---------------------------------
  O1 CSP browser check (30 min)      E2 finish the handler triage
  L1 strict mode: demo, then test    E6 remaining page tests
  O2 Mongo auth: demo, test, mecs    E8 keyboard and focus work
  O7 redis on mecs                   E10 Playwright + axe
  O3 one guided install from zero
  O4 product position
  O5 legal package (counsel)

  Then together: Phase 10 release validation - tag a candidate, deploy from
  scratch, full suite, scans, SBOM, runbook, signed go/no-go record.

  MINIMUM BAR FOR A DEFENSIBLE RELEASE
    - every P0 closed, or covered by a written time-limited acceptance
    - one install from scratch executed, one restore rehearsed  [restore DONE]
    - no silent failure on any path that can lose data
    - marketing claims match what the code does
    - legal package reviewed by counsel


========================================================================
6. THINGS THAT WILL BITE YOU
========================================================================
  Learned the expensive way. Read before touching deployment.

  update-instance.sh does NOT rebuild the image.
      Without --image it regenerates the compose file only. That applied a new
      /api/ready healthcheck against an image predating the endpoint: eleven
      404s, never healthy, automatic rollback. Build first, pass --image.
      A compose healthcheck applies the moment the file is regenerated; the
      image changes only when you ask for it.

  update-instance.sh never writes the instance .env, and uses --no-deps.
      Both deliberate. Rollback restores the compose file and container, so an
      env edit would survive the rollback meant to undo it; and --no-deps
      means it recreates ONLY the app, so a new sidecar (redis) never starts.
      Anything touching .env or sidecars needs its own attended script.

  Mongo only enables auth while initialising an EMPTY data directory.
      Setting MONGO_INITDB_ROOT_* against an existing volume does nothing, and
      says nothing. That is why enable-mongo-auth.sh exists.

  cap_drop: ALL removes CAP_CHOWN.
      The redis entrypoint chowns /data when it starts as root, so the
      container crash-looped on "chown: Operation not permitted". Fixed with
      user: redis. The contract tests passed throughout - they assert the
      service is DECLARED correctly. Only starting it on a real box found it.

  The installer's DNS wait had no timeout when there is no TTY.
      It prompted with a terminal and slept forever without one, so an
      unattended install hung instead of failing. Now bounded
      (DNS_WAIT_MAX_ATTEMPTS). A hang is worse than an error.

  Flask-Limiter: the problem was ordering, not decoration.
      init_rate_limiting runs hundreds of lines before the blueprints are
      registered, so an endpoint lookup there finds nothing and skips in
      silence. apply_expensive_limits() is called after the routes exist.

  vite build does NOT type-check.
      An extraction once shipped a page that could not load. The
      noUndefinedIdentifiers test runs tsc --noEmit and fails on TS2304/TS2552
      only. Run it after any extraction.

  Part numbers are matched case-insensitively throughout. Do not "optimise"
  that away - an $in of compiled case-insensitive regexes is deliberate.


========================================================================
7. STANDING RULES
========================================================================
  DEPLOYMENT REALITY (owner)
    Real companies run TinyMRP now. Breaking things is acceptable. DESTROYING
    DATA IS NOT.
      - never drop, truncate or overwrite existing collections or documents
      - never delete or rewrite files under the deliverables root
      - migrations must be additive and reversible
      - failing loudly at startup is FINE; silently losing a part, a BOM link
        or a deliverable is NOT

  OUT OF SCOPE
    solidworks-addin/ - needs real SolidWorks to test; the owner handles it.
    The mecs instance - the owner said do not touch it.

  PROTECTED PATH
    The guided Caddy deployment must keep working, and on BOTH servers that
    Caddy is SHARED with other production sites. Breaking it takes down more
    than TinyMRP. Anything touching auth, files, containers, health,
    networking, env or deployment must re-check the Caddy contract tests.

  KEEP THE REPOSITORY CLEAN OF INFRASTRUCTURE
    This repo is public. Never commit IP addresses, real customer domains or
    server hostnames. Use aliases and keep the mapping in
    deploy/fleet.local.md, which is gitignored.

  REDUCE FAT PROGRESSIVELY
    Every pass leaves the code leaner: fewer one-use helpers, fewer variables
    that restate the next line, no comment repeating what the code says. Cut
    restatement - keep the reasoning that stops a bug being reintroduced.

  VERIFICATION DISCIPLINE — the rule that produced everything above
    Do not claim a gate is closed because tests pass. Prove the mechanism:
      - the least-privilege Mongo user was verified by trying to read admin
        and being refused
      - the coverage threshold was verified by deleting a test and watching
        CI fail
      - the undefined-identifier guard was verified by removing an import
      - the rate limit was verified by a real 429, after an earlier attempt
        that looked like working code and enforced nothing
      - the backup was verified by restoring it and counting every document
    If a change cannot be proven to work, WITHDRAW it and write down why.
    Something that looks like protection but is not is worse than nothing.

  GATES THAT MUST STAY GREEN
    pytest | ruff check . | bandit -r app -ll
    mypy app/services/security_mode.py app/services/api_auth.py
    cd frontend && npm run test && npx tsc --noEmit -p tsconfig.app.json
