# TinyMRP v2 — runtime dependencies (production).
# Development/test/lint tooling lives in requirements-dev.txt.
# Pins match the previously deployed versions; do not upgrade casually —
# see docs/security/supply_chain_policy.md and the CHANGELOG.

# --- Web framework ---
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
blinker==1.9.0
click==8.4.2
colorama==0.4.6

# --- Auth / security ---
Flask-Limiter==3.8.0
limits==5.8.0
# Redis client for shared rate-limit storage (OPS-RATE-01). Without this,
# Flask-Limiter silently falls back to per-worker memory even when
# RATE_LIMIT_STORAGE_URI points at Redis. Capped below 8.0.0 because
# limits 5.8.0 declares redis<8.0.0.
redis==7.4.1
cryptography==48.0.1
Flask-Login==0.6.3
Flask-Principal==0.4.0
Flask-Security-Too==5.8.1
Flask-WTF==1.3.0
WTForms==3.2.2
email_validator==2.3.0
passlib==1.7.4
libpass==1.9.3
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
cffi==2.0.0
pycparser==2.22

# --- Database ---
mongoengine==0.29.3
pymongo==4.17.0
dnspython==2.8.0

# --- Documents / images (docpacks, thumbnails, labels) ---
pillow==12.3.0
openpyxl==3.1.5
pypdf==6.14.2
reportlab==4.2.5
qrcode[pil]==7.4.2
svglib==1.5.1
defusedxml==0.7.1

# --- HTTP client (files proxy) ---
requests==2.34.2
urllib3==2.7.0
certifi==2025.8.3
charset-normalizer==3.4.9
idna==3.18

# --- Runtime utilities ---
python-dotenv==1.2.2
psutil==7.2.2
tzdata==2026.3

# --- WSGI servers ---
gunicorn==22.0.0        # Linux / containers
waitress==3.0.2         # Windows service (deploy/windows)
