# TinyMRP v2 — runtime dependencies (production).
# Development/test/lint tooling lives in requirements-dev.txt.
# Pins match the previously deployed versions; do not upgrade casually —
# see docs/PROFESSIONALIZATION_PLAN.md Phase 0 and the CHANGELOG.

# --- Web framework ---
Flask==3.1.1
Werkzeug==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.2
itsdangerous==2.2.0
blinker==1.9.0
click==8.2.1
colorama==0.4.6

# --- Auth / security ---
Flask-Login==0.6.3
Flask-Principal==0.4.0
Flask-Security-Too==5.6.2
Flask-WTF==1.2.2
WTForms==3.2.1
email_validator==2.1.2
passlib==1.7.4
libpass==1.9.1.post0
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
cffi==1.17.1
pycparser==2.22

# --- Database ---
mongoengine==0.29.1
pymongo==4.14.0
dnspython==2.7.0

# --- Documents / images (docpacks, thumbnails, labels) ---
pillow==11.3.0
openpyxl==3.1.5
PyPDF2==3.0.1
reportlab==4.2.5
qrcode[pil]==7.4.2
svglib==1.5.1
defusedxml==0.7.1

# --- HTTP client (files proxy) ---
requests==2.32.5
urllib3==2.5.0
certifi==2025.8.3
charset-normalizer==3.4.3
idna==3.10

# --- Runtime utilities ---
python-dotenv==1.1.1
psutil==7.0.0
tzdata==2026.2

# --- WSGI servers ---
gunicorn==21.2.0        # Linux / containers
waitress==3.0.2         # Windows service (deploy/windows)
