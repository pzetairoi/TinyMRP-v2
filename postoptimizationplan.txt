========================================================================
POST-OPTIMIZATION PLAN — what is left, for the owner to prioritise
========================================================================
Opened 2026-08-08. Branch: main.

Follows hardeningplan.txt (closed), posthardeningplan.txt (closed) and
optimizationplan.txt (closed 2026-08-08).

The system is in production and performing acceptably. Nothing below is
urgent. This exists so that whoever picks it up - the owner, or another AI -
can choose what matters rather than rediscover what is left.

WRITTEN FOR A HANDOVER. Each item says what it is, what it costs, and what is
already known, so nobody re-derives anything.


========================================================================
1. WHERE THINGS STAND
========================================================================

  FLEET, by alias. Real addresses in deploy/fleet.local.md, gitignored - this
  repository is public.

    SERVER-A (4 cores, 7.7 GB, comfortable)
      main    PRODUCTION   latest | Mongo authenticated | strict | redis
      vikunja              untouched throughout, verified up
      *** Nextcloud is NOT running. Torn down during the rebuild and never
          reinstalled; cloud.tinymrp.com is unserved. See item 2.1. ***

    SERVER-B (2 cores, 3.8 GB, busy - see 2.4)
      mecs    PRODUCTION   latest | compat mode | no redis
      test                 latest | strict | redis | profiler OFF
      two Nextclouds       the actual resource consumers on this host

  GATES: 787 backend tests, 336 frontend, ruff / mypy / bandit clean, build OK.

  SECURITY POSTURE
    Phase 2 strict authentication is live on main and test. mecs remains on
    compat - one environment variable whenever the owner chooses.
    Mongo authentication is live on main only. demo, test and mecs still run
    unauthenticated databases.
    The CSP browser check (posthardeningplan O1) is still the cheapest open
    security item: about 30 minutes with a browser.


========================================================================
2. WHAT IS LEFT
========================================================================

  2.1 Reinstall the SERVER-A stack, including Nextcloud     VISIBLE GAP
      cloud.tinymrp.com has been unserved since the from-scratch rebuild. It
      is the only thing a user can see is missing. Its data is safe in the
      safety copy taken beforehand: 161 MB under /srv/rebuild-safety-*, with
      the Caddy routes and a Vikunja archive.
      PROCEDURE, learned the hard way:
        - back up, then PROVE the backup restores into a throwaway container
          before destroying anything. Four consecutive backups were once
          silently empty and only verifying caught it
        - Vikunja shares that Caddy and must never go down; check it before,
          during and after
        - the admin password must be at least 12 characters or the container
          refuses to start, having already destroyed the old instance

  2.2 Mongo authentication on demo, test and mecs           SECURITY
      deploy/scripts/enable-mongo-auth.sh exists and is proven on main. One
      short maintenance window each. It backs up first and refuses to proceed
      if the backup fails.
      Note it also breaks backups until they are given credentials - that fix
      is already in backup-instance.sh and restore-instance.sh, but check the
      first backup after each migration actually contains data.

  2.3 Strict mode on mecs                                   SECURITY
      The add-in was verified against strict mode on test, so the risk that
      blocked this is retired. One environment variable and a restart.

  2.4 Nextcloud's resource use on SERVER-B      SUPERSEDED - see section 3
      This item used to say the two Nextclouds took 162% of a 2-core box.
      Re-measured 2026-08-08: that was sampled during a scan window. At rest
      they sit at 0.00-0.01% CPU. The periodic cost is real but it is OUR
      scan job, not Nextcloud, and section 3 has the measurements and plan.
      The RAM claim was also overstated - 934 MB of 1608 MB of containers,
      with 1674 MB available and zero swap.
      "Nothing in TinyMRP will move this number" was wrong twice over: the
      number was mostly ours to begin with, and N2 removes most of it.
      What survives from this item: the host is not out of memory. Linux
      counts page cache as used; `available` is the number that matters.

  2.5 _flatten_bom level batching                           PERFORMANCE
      A doc-pack build measured 22 s and 6625 BOM queries before a
      children-lookup memo was added. NOT re-measured since - do that first,
      the memo may have taken most of it.
      CARRIES REAL RISK: quantities accumulate per path, and $graphLookup
      reports reachability rather than multiplicity, so it cannot be used
      here. A wrong quantity on a document pack is a manufacturing error, not
      a slow page. tests/test_exact_bom_pairs_batching.py already asserts a
      part reached via two paths at 2x3 and 5x3 totals 21; keep it green.

  2.6 The remaining tree walks                              PERFORMANCE
      Nine were inventoried in optimizationplan.txt P-25. Only touch ones the
      profiler shows are hot; half run once a day.

  2.7 Frontend test coverage                                QUALITY
      4 of 8 pages still have no page-level test. Playwright and axe are still
      not installed, which also blocks automated accessibility checks and any
      testing of the 3D viewer and canvas markup.

  2.8 Silent exception handlers                             QUALITY
      158 of 184 remain, concentrated in docpacks.py, admin.py, parts.py and
      __init__.py. The 26 already triaged surfaced two real defects, so the
      exercise pays - but the rest are mostly best-effort UI paths.

  2.9 Commercial and legal                                  OWNER
      Drafts are in docs/commercial/ and need counsel. The licence decision -
      The Unlicense, public domain - means the agreement must be a service and
      support contract, not a EULA. Restrictions cannot be imposed on software
      dedicated to the public domain.

  2.10 Help screenshots                                     COSMETIC
      Five placeholder images in the running help state what to capture and
      which file to overwrite. Replace them one at a time. Drawing markups
      have no help section at all - a whole feature undocumented.


========================================================================
3. NEXTCLOUD RESOURCE PHASE
========================================================================
  Opened 2026-08-08 after an external review (ChatGPT) of Nextcloud's
  resource use. The review's direction was right; its magnitudes were not.
  Everything below was MEASURED read-only on SERVER-B before being written
  down. No change has been made yet.

  3.1 WHAT THE MACHINE ACTUALLY SHOWS

    Memory, SERVER-B, 3819 MB total:
      used 2145 | buff/cache 1553 | AVAILABLE 1674
      swap 0 used, 0 pages ever swapped, 0 OOM kills in dmesg

    Containers by RSS:
      tinymrp-nextcloud-db          273.5 MB    0.01% CPU
      tinymrp-test-nextcloud-app    228.4 MB    0.00% CPU
      tinymrp-test-nextcloud-db     225.0 MB    0.01% CPU
      tinymrp-test-app              214.4 MB    0.11% CPU
      tinymrp-nextcloud-app         207.6 MB    0.00% CPU
      tinymrp-mecs-app              166.7 MB    0.12% CPU
      tinymrp-mecs-mongo            136.4 MB    0.39% CPU
      tinymrp-test-mongo            120.1 MB    0.41% CPU
      tinymrp-caddy                  31.1 MB    0.00% CPU
      tinymrp-test-redis              4.2 MB    0.47% CPU

    THREE CORRECTIONS TO THE PREMISE. Worth stating plainly because the
    whole plan changes shape once they are true:

    a) Nextcloud is NOT using 80% of RAM. It is 934 MB of 1608 MB of
       containers - 58% - and the host has 1674 MB available with zero
       swap and zero OOM kills. There is no memory pressure to fix.

    b) Nextcloud is NOT using 80% of CPU. At rest it is 0.00-0.01%. The
       162% figure recorded earlier in optimizationplan.txt was sampled
       DURING a scan window. Both the earlier measurement and the external
       review generalised a spike into a steady state.

    c) The CPU is real, but it is periodic and it is entirely ours. It is
       the scan job we install, not Nextcloud running hot.

  3.2 THE ACTUAL COST, MEASURED

    Three consecutive runs of each scan, from the journal:
      mecs   56.1s  51.5s  52.1s
      test   38.7s  30.8s  30.5s

    Both timers fired at 1786157409.640 and 1786157409.645 - SIX
    MILLISECONDS apart. They are not staggered and they never will be:
    install-backup-job.sh sets RandomizedDelaySec=600, and
    install-nextcloud-scan-job.sh sets nothing at all.

    Duty cycle: 52s + 31s of concurrent work every 300s on two cores.
    Roughly 28% of the machine's wall-clock time, almost always computing
    that nothing has changed.

    One mecs run broken down:
      02:50:10  mount visibility check
      02:50:13  group user discovery
      02:50:39  files_external:scan 1          26s
      02:51:01  files:scan --path=admin/...    22s
      total 51s

    THE SECOND PASS RE-WALKS THE TREE THE FIRST PASS JUST WALKED. Same
    files, once as the external mount and once through the user's view.
    About half the cost, and nobody has ever tested whether dropping it
    changes what appears in the Nextcloud UI. That test is item 3.5.

    Good news the review guessed at and could not confirm: the targeted
    path is working. "files_external:scan --all fallback used: no" and
    "files:scan --all fallback used: no" on every run. The catastrophic
    fallback exists but is not firing.

  3.3 THE IDEA THAT DOES NOT SURVIVE CONTACT WITH THE CODE

    The review proposes that TinyMRP mark the mount dirty on write and
    trigger a targeted scan. The instinct is right. The mechanism cannot
    be built as described:

      TinyMRP HAS NO NEXTCLOUD AWARENESS AT ALL. Grepping the whole of
      app/ for "nextcloud" returns two hits, both in static help HTML.
      The integration lives entirely in deploy/scripts.

      THE APP CANNOT RUN occ. It is a non-root container with no docker
      socket. Mounting the socket to let it exec into the Nextcloud
      container would hand the application root on the host and undo the
      hardening plan in a single line. Not a trade worth making to avoid
      a filesystem scan.

    The safe equivalent, and what 3.6 specifies, is a marker file in the
    deliverables directory - which is ALREADY a shared bind mount, so it
    needs no new privilege, no new network path and no new socket.

  3.4 AND THE CONCERN THAT IS ALREADY SOLVED

    The worry was that a user dropping files into their Nextcloud would
    not be seen by a subsequent import. Verified in app/services/
    filescan.py: discovery reads the filesystem directly through
    iterdir(), and the directory index cache is documented as living for
    exactly one scan "because storage changes between scans".

    So an import already sees those files IMMEDIATELY. It does not
    consult Nextcloud's file cache and never has.

    The occ scan only serves the OPPOSITE direction: making deliverables
    that TinyMRP wrote appear in the Nextcloud web UI. Nothing about
    import speed or correctness depends on it. That reframes the whole
    job - it is a convenience refresh, not a correctness mechanism, and
    it can be made lazy without risking an import.

  3.5 THE PLAN, CHEAPEST AND SAFEST FIRST

    N0  STAGGER THE TIMERS                     DONE 2026-08-08 (5c0f2c4)
        RandomizedDelaySec (half the interval, bounded 15-300s) plus
        AccuracySec=15s in install-nextcloud-scan-job.sh, matching what
        install-backup-job.sh already did. Live on test.

    N1  RAISE THE DEFAULT INTERVAL             REJECTED BY OWNER 2026-08-08
        Was: 5 -> 15 minutes as a holding measure. The owner does not
        want freshness to get worse, and he is right to refuse it: N2
        makes the idle scan nearly free, so the interval should go DOWN
        to 1 minute, not up. Raising it would have traded the thing that
        matters to users for the thing the guard fixes anyway.
        Do not revive this item. Go straight to N2.

    N2  CHANGE-DETECTION GUARD    IMPLEMENTED 2026-08-08 (5c0f2c4), ON TEST
        Revised 2026-08-08. An earlier draft had TinyMRP touch a marker
        file. That is unnecessary: the scan job runs on the HOST, and the
        deliverables tree is on the host. It can detect change itself.

        MEASURED ON TEST AFTER DEPLOYING:
          unchanged tree          29s -> 0s by hand
          file created            scans, and occ info:file confirms the
                                  file is indexed (fileid issued)
          file deleted            scans, and occ info:file confirms it is
                                  gone from Nextcloud
          interval                5 min -> 1 min, with 30s jitter
          doctor --instance test  0 failures

        THEN UNDER THE REAL TIMER, from the journal:
          0.24s and 0.15s per run, against 30.5-38.7s before.
          Duty cycle for test: ~10.3% of the machine -> ~0.3%.
        Roughly 150x less work, while checking five times more often.

        Verified in Nextcloud itself, not in the exit code - the scan
        reports success either way, which is exactly how a guard like
        this would hide a regression.

        mecs is deliberately NOT on it yet. A systemd drop-in pins it to
        the old unconditional behaviour:
          /etc/systemd/system/tinymrp-nextcloud-scan-global-mecs.service.d/
            keep-unconditional.conf
          Environment=TINYMRP_NEXTCLOUD_SCAN_MAX_SKIP_SECONDS=0
        Zero makes the hourly-safety-net branch fire every time, which is
        precisely the pre-guard behaviour. Delete that file, reload and
        relink mecs at 1 minute to adopt it.

          - keep a stamp file per link, e.g.
            /var/lib/tinymrp/nextcloud-scan/<instance>.stamp
          - the job starts by asking the filesystem whether anything is
            newer than the stamp; if not, exit
          - only if something changed: touch the stamp FIRST, then scan,
            so a write during the scan is not swallowed

        Why this is the right shape:
          - NO application change. No new code path in TinyMRP, no
            rebuild, no redeploy, no docker socket, no new privilege.
            It is a guard clause in a shell script that already exists.
          - a no-op costs a metadata walk in milliseconds instead of
            ~50 seconds of PHP - roughly a 250x reduction on the case
            that is true almost every time
          - because the idle case is nearly free, the interval can go
            DOWN to 1 minute rather than up. The owner gets faster
            freshness than today AND the machine gets its CPU back.
          - it catches changes made by anyone - TinyMRP, a user in the
            Nextcloud UI, an admin over SSH - not only writes TinyMRP
            knows it made

        Care: use the cheapest check that is still correct. A full
        -newer stat of every file is fine at current sizes but grows
        with the tree; directory mtimes alone miss in-place edits.
        Measure the guard itself before trusting it, and keep a slow
        unconditional scan (hourly, staggered) as a safety net.

        Item 3.6 then reduces to almost nothing: the import does not
        need to signal anything, because the guard already sees what
        the import wrote.

    N3  DROP THE REDUNDANT SECOND PASS         measure, then decide
        Test whether files:scan --path adds anything after
        files_external:scan on the same tree. If not, halve the cost of
        every scan. VERIFY IN THE NEXTCLOUD UI, not in the exit code -
        the scan reports success either way.

    N4  NEXTCLOUD'S OWN BACKGROUND JOBS        correct, and missing
        Confirmed: backgroundjobs_mode is unset on both instances, so
        both are on the AJAX default and a user's page load can drag
        Nextcloud's housekeeping. Set background:cron with a real cron
        entry, plus maintenance_window_start so the heavy daily jobs run
        at night. Note this ADDS a small steady cost and removes a
        larger unpredictable one.

    N5  STOP THE TEST NEXTCLOUD WHEN NOT TESTING    DECLINED BY OWNER
        453 MB and two containers, idle almost always. Declined
        2026-08-08: the owner wants test Nextcloud available. Fine -
        there is no memory pressure (1674 MB available, zero swap), so
        this was convenience, not need. Left here only so nobody
        proposes it again as if it were new.

    N6  REDIS FOR NEXTCLOUD                    DEPRIORITISED - see why
        The review recommends it for locking and distributed cache,
        citing the MariaDB CPU. That CPU was scan-window sampling; at
        rest both databases sit at 0.01%. Redis would mostly relieve
        load that N2 deletes outright. Two more containers and two more
        failure modes to fix a symptom of the thing being removed.
        Revisit only if N2 lands and the databases are still busy.

    N7  APACHE / PREVIEWS / INNODB TUNING      only with evidence
        The review is right that mod_php + prefork is not the leanest
        model, and right that preview generation can spike to 256 MB.
        But nothing here is currently under pressure. MariaDB's 128 MB
        default buffer pool already explains the ~225-275 MB RSS, so
        setting innodb_buffer_pool_size=128M would very likely save
        nothing. Measure with SHOW VARIABLES before touching it.

    N8  PHP-FPM WITH pm=ondemand               architecturally nicer, later
        A genuinely better fit for many tenants with few concurrent
        users. It is also a migration of the web server under a running
        production service, for a host that is not short of memory.
        Last, if ever.

  3.6 SEPARATE TASK: SCAN ON IMPORT            RISKY - own change, own test

    The owner's other idea: when an import runs, discover filesystem
    changes so new file references are usable straight away, WITHOUT the
    sync blocking or delaying the import, and ideally with images first
    so thumbnails can be built.

    Flagged by the owner himself as potentially problematic, and he is
    right. Recorded here rather than folded into N2 because it touches
    the import path, which is the one thing in this system that must not
    become slower or less correct.

    What is already true (3.4): the import ALREADY sees new files on
    disk. So this task is not about correctness. It is about making the
    Nextcloud side refresh at the moment there is a reason to.

    The cheap, safe 90%: have the import touch the N2 marker when it
    finishes writing. The scan then happens within a minute, entirely
    outside the request, and the import never waits.

    What to be careful about, if it ever goes further than that:
      - the import must not block on any scan, ever
      - a failed or slow scan must not fail an import
      - thumbnail generation on the TinyMRP side is already an
        import-time concern (see optimizationplan.txt); do not entangle
        the two mechanisms just because both involve images
      - re-imports storage-scan every planned part, so an import can
        write a great many files in a burst: one marker, one scan, not
        one per file


========================================================================
4. HOW TO WORK ON THIS SYSTEM
========================================================================
  Distilled from three plans and several expensive mistakes. This is the part
  worth reading before touching anything.

  MEASURE FIRST. TINYMRP_PROFILE=1 enables a per-request profiler that counts
  Mongo operations - count, not time, because every query here already runs in
  about a millisecond and the damage is doing it four hundred times. It costs
  about 31% CPU under load, so turn it off afterwards. Two optimisations in
  this project targeted the wrong thing because the assumption sounded right.

  PROVE A BACKUP RESTORES BEFORE DESTROYING ANYTHING. Four consecutive backups
  were silently empty after Mongo authentication was enabled, and the guard
  passed them because it checked file size rather than content. A backup that
  has not been restored is a file, not a backup.

  DO NOT REMOVE AN AUTHORISATION CHECK BECAUSE IT LOOKS REDUNDANT. Tried twice
  here, caught twice by tests, once turning a 403 into a 200 on a
  path-traversal record. Make checks cheap instead - resolve the data once and
  hand it in. That worked every time.

  COUNT THE TENANTS BEFORE MULTIPLYING ANYTHING PER INSTANCE. Raising gunicorn
  workers with the standard formula took a shared host down and made it
  unreachable. Nothing inside a container can see its neighbours; only the
  operator can. WEB_CONCURRENCY is the per-host override and exists for this.

  WORK BELONGS AT IMPORT, QUERIES SHOULD ONLY READ. Before writing a batch
  query, check whether import already computed the answer onto the document.
  part.file_groups and canonical.approved are both materialised and indexed.

  DESTROYING DATA IS NOT ACCEPTABLE. Breaking things is. Never drop, truncate
  or overwrite existing collections, documents or deliverables. Migrations
  must be additive and reversible. Failing loudly at startup is fine; silently
  losing a part, a BOM link or a deliverable is not.

  THE CADDY IS SHARED. On both servers it fronts other production services -
  Vikunja on one, two Nextclouds on the other. Breaking it takes down more
  than TinyMRP.

  solidworks-addin/ IS OUT OF SCOPE. It needs real SolidWorks to test.

  KEEP INFRASTRUCTURE OUT OF THE REPOSITORY. It is public. Aliases only; the
  real mapping lives in deploy/fleet.local.md, which is gitignored.

  GATES THAT MUST STAY GREEN
    pytest | ruff check . | bandit -r app -ll
    mypy app/services/security_mode.py app/services/api_auth.py
    cd frontend && npm run test && npx tsc --noEmit -p tsconfig.app.json
