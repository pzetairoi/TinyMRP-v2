========================================================================
STATUS LOG — keep this current so work is not repeated
========================================================================
Branch: hardening/production-readiness
Driver: Codex. Anything below marked [DONE ... Claude] was done in parallel.
Phase 0 is complete; no Phase 0 files remain reserved for parallel work.

========================================================================
PARALLEL WORK BOARD — update before editing; never share an unclaimed file
========================================================================
Coordination rules:
  1. Each AI must claim a bounded workstream and its files here before editing.
  2. A parallel AI should use its own Git worktree and branch from the current
     hardening/production-readiness HEAD. It must not switch the branch in the
     primary workspace or stage/commit another workstream's files.
  3. Do not edit a file reserved by another active workstream. Send findings to
     the primary owner instead.
  4. Each workstream keeps one focused commit, records commands/results here,
     and identifies the commit hash for review/cherry-pick.
  5. The primary owner integrates commits, runs the combined full suite and
     updates the production-blocker register.

Active workstreams as of 2026-08-03:
  P1C-SESSION-LIFECYCLE  IN PROGRESS  owner: Codex /root
    Scope: central fs_uniquifier rotation, browser-session invalidation for
           password/security-state changes, lifecycle audit and regressions.
    Reserved files:
      app/services/session_lifecycle.py (new)
      app/views/main.py
      app/views/admin.py
      app/cli.py
      app/services/rls_demo.py
      tests/test_session_lifecycle.py (new)
      tests/test_account_profile.py
      tests/test_admin_access_usability.py
      tests/test_cli_security.py
      docs/PRODUCTION_HARDENING_BASELINE.md
      CHANGELOG.md
      hardeningplan.txt
  P3B-RUNTIME-UPGRADES  IN PROGRESS  owner: /root/p3b_runtime_upgrades
    Scope: Pillow 12.3.0, gunicorn 22.0.0 and cryptography 48.0.1 pins plus the
           required cffi 2.0.0 compatibility pin; focused compatibility/security
           tests and protected Caddy evidence.
    Reserved files:
      requirements.txt
    Read-only verification may use all tests and deployment scripts. The owner
    must work in a separate worktree/branch, must not edit application/auth code,
    tests, generated assets, docs, manifests other than requirements.txt, or this
    plan, and must return one commit for primary review.
    FINDING cryptography 48.0.1 requires cffi>=2.0.0 on CPython; retaining the
            prior cffi 1.17.1 pin makes the production Docker resolver fail with
            ResolutionImpossible. cffi 2.0.0 is therefore an in-scope required
            compatibility update, not an unrelated dependency refresh.

Completed workstream scope:
  P1B-TOKEN-LIFECYCLE  DONE 2026-08-03  owner: Codex /root
    Scope: token ownership/active-user enforcement, bounded lifetime, rotation,
           revocation triggers, management UX, audit trail and regression tests.
    Files formerly reserved (released after the final evidence commit):
      app/__init__.py
      app/cli.py
      app/models/api_token.py
      app/services/api_auth.py
      app/services/audit.py
      app/services/api_tokens.py
      app/services/rls_demo.py
      app/views/admin.py
      app/views/main.py
      app/views/me.py
      app/templates/admin/**
      app/templates/me/**
      frontend/src/pages/ApiTokensPage.tsx
      frontend/src/pages/AdminAddinPage.tsx
      app/static/parts-ui/** (generated from the claimed frontend sources)
      docker-compose.yml
      docker-compose.onefolder.yml
      tests/test_api_auth.py
      tests/test_admin_access_usability.py
      tests/test_token_lifecycle.py (new)
      .env.dev.example
      .env.docker.example
      .env.server.example
      README.md
      docs/help/07_troubleshooting.md
      app/static/help/help.html
      app/static/help/help_toc.json
      docs/PRODUCTION_HARDENING_BASELINE.md
      CHANGELOG.md
      hardeningplan.txt
    Integration gate: full Python suite plus the protected Phase 3A VPS/Caddy
                      contract, config-render and deployment-script checks.

Completed workstreams:
  P1B-TOKEN-LIFECYCLE  DONE 2026-08-03  commits: 49cf24a, 49ee2b4
    Expiring tokens, owner enforcement, lifecycle UI, revocation triggers,
    global API-token logout, full suite and protected VPS/Caddy gates.
  P3A-BOOTSTRAP  DONE 2026-08-03  owner: Codex /root
    Canonical, idempotent, fail-closed bootstrap; deliberate credentials;
    fresh-Compose smoke; standalone/one-folder migration; operator docs.
  P3A-CADDY-TEST  DONE 2026-08-03  owner: /root/phase3a_caddy_audit
    Independent audit plus six cross-platform regression contracts in
    tests/test_vps_caddy_deployment_contract.py. No Caddy defect was exposed.
  P3A-HANDOFF  DONE 2026-08-03  owner: Codex /root
    Final consistency sweep, Python 3.11 verification, evidence and roadmap.
  P3B-ADVISORY-TRIAGE  DONE/INTEGRATED 2026-08-03  owner: Claude + Codex review
    Security docs originated at 6912f78 and were reviewed, corrected and
    integrated on hardening/production-readiness as 50d75e7 plus the following
    reconciliation checkpoint. No manifest or application code changed.

Parallel workstream definition (complete; retained for scope history):
  P3B-ADVISORY-TRIAGE  DONE and integrated on hardening/production-readiness
    Scope: applicability research and time-limited exception templates for the
           current Python/React Router advisories; documentation only.
    Allowed files:
      docs/security/dependency_advisory_triage.md (new)
      docs/security/risk_acceptance_template.md (new)
    Do not change requirements, package manifests, workflows or application
    code in this stream. Record sources, affected features, compensating
    controls, owner and expiry for primary review.

Next integration order:
  Phase 1C session lifecycle is the next primary workstream. The independent
  Pillow/gunicorn/cryptography runtime-pin upgrade may run in parallel once
  claimed; PyPDF2-to-pypdf remains a separate later workstream.
========================================================================

Completed so far:
  Phase 1B  DONE 2026-08-03 (Codex; implementation checkpoint by user)
                   IAM-TOKEN-01 closed; IAM-REV-01 API-token portion closed;
                   session portion remains Phase 1C
  Phase 3A  DONE 2026-08-03 (Codex + parallel Caddy audit)
                   DEPLOY-SEED-01 closed; evidence below and in
                   docs/PRODUCTION_HARDENING_BASELINE.md
  Phase 1A  DONE   stored XSS in job/order forms + 17 regression tests (Claude)
                   residual: CSP unsafe-inline removal NOT started
  Phase 3B  PARTIAL earlier dependency upgrades plus completed advisory triage
                   (Claude + Codex review; triage integrated as 50d75e7)
                   residual: four remediable package changes remain; two
                   proposed exceptions need a human owner/date before release
  Phase 3C  PARTIAL actions on current majors, but SHA pinning + digest pinning
                   + blocking gates all still TODO
  Phase 0   DONE 2026-08-02 (Codex)
                   baseline tag: hardening-baseline-2026-08-02
                   evidence: docs/PRODUCTION_HARDENING_BASELINE.md

Phase 0 completion details:
  DONE Created hardening/production-readiness from origin/main f574515.
  DONE Classified dependency/workflow/generated-bundle commits 5a893e6 and
       f574515 as intentional and preserved them.
  DONE Added LF attributes for frontend/build assets and shell scripts so a
       Windows checkout does not create spurious generated-bundle changes.
  DONE Fixed clean-start runtime secrets: lock acquisition creates a missing
       parent directory before creating runtime_secrets.json.lock.
  DONE Fixed pinned Flask-Security/Werkzeug cache-control compatibility. The
       app now emits standard `private, no-store` directives, and the old exact
       string test is semantic. The formerly known failure is resolved.
  DONE Clean CPython 3.11.11 environment: 568 passed, 1 skipped, 585 warnings,
       71.04% coverage; dependency compatibility check passed.
  DONE Node 24.18.1/npm 11.19.0: clean install, lint (one warning), build; two
       consecutive builds were byte-stable.
  DONE Ruff configured rules, scoped mypy and Bandit -ll pass. Black remains
       non-blocking with 162 files requiring formatting.
  DONE Main/one-folder Compose parse, production image build, 62 add-in tests,
       shell syntax, isolated Caddy render/validate and rendered VPS Compose.
  DONE Added the audit baseline and explicit release-blocker checklist under
       docs/PRODUCTION_HARDENING_BASELINE.md.
  OPEN Supply-chain gates: pip-audit 34 entries/5 packages; npm production
       audit 2 high entries. These are Phase 3B blockers.

Phase 3B advisory-triage record (integrated 2026-08-03):
  EVIDENCE docs/security/dependency_advisory_triage.md and
           docs/security/risk_acceptance_template.md, integrated in 50d75e7
           and corrected against the current Phase 1B tree in the following
           documentation checkpoint.
  VERIFIED pip-audit remains 34 rows across five packages. cryptography accounts
           for five rows but four distinct advisory IDs because PYSEC-2026-35 is
           duplicated. npm production audit remains two high React Router rows.
  ACTION Pillow 11.3.0 -> 12.3.0 is highest priority: user-controlled images
         reach native decoders. Test upload, thumbnails, markup and docpacks.
  ACTION gunicorn 21.2.0 -> 22.0.0 addresses request smuggling relevant behind
         the protected Caddy proxy. Re-run rendered VPS Compose/Caddy and the six
         deployment-contract tests.
  ACTION cryptography 43.0.1 -> 48.0.1 clears the bundled-OpenSSL and related
         rows. Keep these three straight runtime upgrades in one bounded commit.
  ACTION Replace EOL PyPDF2 with pypdf in a separate commit. There are 12 import
         sites: seven in docpacks.py, four in order_scope.py and one in
         markup_documents.py. Test crafted/invalid PDFs and binder generation.
  EXCEPTION EXC-2026-001 is Proposed only. Flask-Security-Too's medium advisory
            is WebAuthn-only; TinyMRP neither enables WebAuthn nor installs its
            extras. No patched release exists. Enabling WebAuthn voids it.
  EXCEPTION EXC-2026-002 is Proposed only. The React Router high advisory is
            RSC-only and TinyMRP is a client SPA without unstable RSC APIs. Core
            react-router 8.3.0 is fixed, but react-router-dom has no 8.x release.
            Do not run npm audit fix --force: its proposed 7.11.0 downgrade
            restores five other advisories.
  BLOCKED Both exception records require a named human risk owner, acceptance
          date and <=90-day expiry before they can satisfy a release gate.
          Engineering must not mark them Accepted on the owner's behalf.

Phase 3A completion details:
  DONE Replaced the inline legacy role seed and removed the missing PERMISSIONS
       import. app/services/container_bootstrap.py now uses the canonical
       reconcile_standard_roles() create-missing mode and never overwrites role
       drift, deletes custom/legacy roles, or changes an existing user.
  DONE Fresh opt-in admin bootstrap validates explicit email/password before any
       write, rejects the historical example password, hashes the password and
       grants only the canonical administrator role. No credential is generated
       or printed in container logs.
  DONE Entrypoint distinguishes deterministic configuration failure from
       retryable runtime/database failure, bounds retry behavior and exits
       nonzero before Gunicorn after the final failure.
  DONE Direct Compose seeding now defaults false. Guided Caddy create-instance
       keeps its existing explicit seed/email/password env contract. The Windows
       one-folder helper generates/stores credentials operator-side, preserves
       them on rerun and safely detects older data without stored credentials.
  DONE Canonical administrator assignment also replaces new legacy-admin
       creation in CLI grant/bootstrap commands. Existing legacy admin users are
       intentionally untouched for compatibility.
  DONE Fixed standalone install-server.sh's invalid CLI invocation. It now uses
       the same safe bootstrap, passes operator values as positional shell
       arguments, avoids password reset on reinstall and fails the requested
       installation when first-admin creation fails.
  DONE Added 20 bootstrap/installer tests, six independent VPS/Caddy deployment
       contract tests and a CI fresh-Compose job. The smoke checks Mongo, exact
       roles, admin, CSRF browser login, health, protected bearer API, restart
       idempotence, log secrecy and fail-closed invalid bootstrap.
  VERIFIED Linux Python 3.11 final suite: 595 passed; Phase 3A focused final
       suite: 26 passed. Ruff, pinned Black, scoped mypy, Bandit -ll, pip check,
       Bash syntax, ShellCheck -S error, PowerShell parse and workflow YAML/
       embedded Bash parse all pass.
  VERIFIED Built production image and real fresh stack: Mongo ready; exact
       canonical roles; administrator; browser login; /api/health; protected
       /api/auth/check; password absent from logs; restart password hash/roles/
       counts unchanged; invalid fresh config exited 2 without launching app.
  VERIFIED Main/one-folder Compose parse. All deployment scripts pass Bash
       syntax. A representative internal-TLS Caddy route passes caddy validate,
       the rendered VPS Compose passes docker compose config, and the empty
       FILES_ACCEL_REDIRECT_PREFIX/update/rollback/doctor contracts have tests.
  NOTE No live public VPS, DNS or ACME mutation was performed. That remains a
       release-environment validation item, not a Phase 3A code defect.

Protected owner requirement added 2026-08-02:
  The guided Ubuntu/VPS deployment scripts using Caddy MUST remain supported.
  Any phase touching auth, files, containers, health, networking, environment
  or deployment must validate Bash syntax, rendered Caddy config, rendered
  instance Compose, empty FILES_ACCEL_REDIRECT_PREFIX, update/rollback behavior
  and fresh bootstrap. Do not replace the Caddy path with Nginx-only behavior.

Current work:
  Phase 1B is DONE and P3B advisory triage is integrated. Phase 1C session
  lifecycle is next. The straight P3B runtime upgrades are safe to assign to a
  separate worktree after the board records non-overlapping file ownership.

Phase 1B decision log (recorded before implementation, 2026-08-03):
  * New API tokens expire after 90 days by default and may not exceed 365 days;
    both values are operator-configurable, validated positive integers and the
    default may never exceed the maximum.
  * Existing non-expiring tokens remain usable for a controlled compatibility
    window, but are labelled legacy/no-expiry in both user and administrator UI
    so operators can rotate them. Phase 1B does not silently break deployed
    SolidWorks integrations.
  * Ordinary self-service password changes revoke every API token, as do admin
    password resets, CLI password resets, permission-test credential refreshes
    and account deactivation. Reactivation never restores revoked tokens.
  * Rotation is user-controlled, returns the replacement secret once, and
    guarantees that a concurrent loser token is revoked. Administrators may
    explicitly revoke one user's token or all active API tokens, but may not
    mint or retrieve another user's secret.
  * Phase 1B closes API-token portions of IAM-REV-01. IAM-REV-01 itself remains
    open until Phase 1C implements server-side session invalidation.

Phase 1B completion record (2026-08-03):
  IMPLEMENTED Additive token lifecycle metadata/index, active/missing owner
              enforcement, 90/365-day validated policy, legacy visibility,
              one-time-secret rotation, idempotent revoke helpers and global
              API-token logout.
  IMPLEMENTED User and administrator React UI now show expiry, last use and
              active/expired/revoked/legacy status. Users can rotate/revoke;
              administrators can revoke one or all but cannot mint user tokens.
  IMPLEMENTED Automatic revocation on self-service/admin/CLI password changes,
              bulk/single deactivation and disposable test-user credential
              refresh. Bearer-token audit events now retain the acting user.
  IMPLEMENTED Config examples and both Compose paths expose positive validated
              API_TOKEN_DEFAULT_TTL_DAYS/API_TOKEN_MAX_TTL_DAYS. Guided VPS
              Compose already loads its per-instance env file and needs no
              renderer change; default behavior therefore remains compatible.
  TESTED 10 new lifecycle regressions; 71 lifecycle/auth/account/admin/CLI
         tests pass. Help + Phase 3A bootstrap/VPS-Caddy contract set: 30 passed.
         Full host suite: 604 passed, 1 skipped. Production-image Python 3.11
         mounted-source suite exited 0 with the same test set. Frontend lint
         passes with one pre-existing unrelated warning; production bundle
         builds successfully. Help and frontend generated assets updated.
  CHECKPOINT User committed and pushed implementation as 49cf24a to
             origin/hardening/production-readiness while verification ran;
             verification/type checkpoint 49ee2b4 is also pushed.
  VERIFIED Ruff, scoped mypy, changed-file Black, Bandit -ll, pip check,
           frontend lint/build, gitleaks, Bash syntax, ShellCheck error-level,
           PowerShell parse, main/one-folder/rendered VPS Compose and rendered
           internal-TLS Caddy all pass.
  VERIFIED A clean production build and isolated fresh stack passed canonical
           bootstrap, default 90-day token, login, health, protected API,
           restart, log secrecy and fail-closed invalid-bootstrap checks.
  RESIDUAL Legacy no-expiry tokens remain identifiable and valid until rotated;
           browser-session revocation is Phase 1C; strict browser/API compatibility
           is Phase 2; add-in secret storage is Phase 7.
  NEXT Claim and implement Phase 1C. In parallel, upgrade only Pillow, gunicorn
       and cryptography in a separately owned P3B runtime-pin workstream.
========================================================================

Progressive mitigation roadmap
The recommended sequence below keeps each phase reviewable and deployable. Phases 0–5 are production blockers; phases 6–10 build commercial maturity.
Each phase should end with:
A focused change set.
Automated regression tests.
Updated deployment or operator documentation.
A short residual-risk report.
A clean commit suitable for rollback.
Phase 0 — Establish a controlled baseline — [DONE 2026-08-02, Codex]
Objective: Create an immutable, reproducible starting point before remediation begins.
Completion evidence and residual risks are in
docs/PRODUCTION_HARDENING_BASELINE.md. All listed Phase 0 acceptance criteria
were met. The Caddy/VPS fresh-bootstrap limitation was recorded as a Phase 3A
production blocker and was closed on 2026-08-03.
Estimated effort
0.5–2 engineering days.
Request to use
Completed. Do not repeat Phase 0; preserve hardening-baseline-2026-08-02.

Phase 1 — Eliminate immediate browser and identity vulnerabilities
Objective: Close the highest-impact attack paths without redesigning the whole authentication system.

1A. Stored XSS remediation — [DONE 2026-08-02, Claude, branch hardening/production-readiness]
Completed in parallel with Codex's Phase 0; no overlap with Phase 0 files.
  DONE Replaced template-string innerHTML construction in the job and order forms.
       - app/templates/admin/jobs_form.html   renderBomTable()
       - app/templates/admin/orders_form.html renderOrderTable()
       Rows are now built with createElement + textContent/property assignment.
       Confirmed stored (not merely reflected): row.desc is populated from the
       /api/part_detail response, so a hostile part description executed in every
       job/order referencing that part. In orders, row.uom and row.note also broke
       out of value="..." attributes.
  DONE Image paths treated as structured values: safeImageSrc() in both templates
       allows only http(s) or same-origin relative paths, rejects protocol-relative
       "//host", and falls back to the branding logo. Part links keep
       encodeURIComponent and are built as <a> nodes.
  DONE Swept all templates and frontend code for the other sinks. Findings:
       - insertAdjacentHTML / outerHTML / document.write: none in app code.
       - dangerouslySetInnerHTML: none in frontend/src.
       - javascript: URLs: none in frontend/src.
       - Remaining .innerHTML uses are `= ''` constant clears only (safe).
       - One {{ ... |safe }} remains at app/templates/help/index.html:100
         (server-authored help content, not user input) — see Phase 1A residual below.
  DONE Regression tests: tests/test_stored_xss_regression.py (17 tests).
       Source-level invariants, so they pin the *class* of bug rather than one
       payload. Verified they FAIL against the pre-fix templates and pass after.
       Includes a repo-wide guard against new unsafe innerHTML assignments.
  VERIFIED Jinja parses in both templates; rendered JS is syntactically valid via
       `node --check` across all four {% if %} branch combinations (jobs
       edit/readonly, orders with/without can_read_financial). Full suite:
       567 passed, 1 failed — that failure is the pre-existing Cache-Control
       assertion in test_admin_access_usability.py, unrelated to this change.

1A residual / NOT done (deliberately left):
  TODO "Begin removing the need for CSP unsafe-inline."
       Not started. These templates still use inline <script> blocks and a few
       inline onclick= handlers (e.g. jobs_form.html setCheckboxGroup buttons),
       so unsafe-inline is still required. Removing it means moving these blocks
       to external files with a nonce/hash strategy — a separate change that
       touches the CSP header and every admin template, and is better done after
       Phase 2 settles the auth surface.
  NOTE inline event attributes (onclick=) still exist in admin templates. They are
       author-written with no untrusted interpolation, so they are not an XSS sink
       today, but they are what blocks CSP tightening above.

1B is DONE as recorded above; 1C is NOT started.
1B. API-token lifecycle — [DONE 2026-08-03, Codex]
Require the token owner to be active on every authenticated request.
Reject tokens when their user no longer exists.
Add default and maximum token lifetimes.
Display expiry and last-used information in the token UI.
Add token rotation and explicit revocation.
Revoke tokens when:A user is deactivated.
A password is reset.
A security administrator requests global logout.

Decide whether an ordinary password change revokes all tokens or offers the user that option.
1C. Session lifecycle
Rotate fs_uniquifier or introduce an equivalent session-version field when credentials or security state change.
Invalidate existing browser sessions after administrative password reset or deactivation.
Audit these security actions.
Test active, inactive, deleted, expired and revoked cases.
Acceptance criteria
Stored values cannot create executable DOM in affected forms.
Deactivated users immediately lose browser and API access.
New user tokens expire by default.
Password-reset behavior is explicit and tested.
Security lifecycle events appear in the audit log.
Existing authorized workflows continue to work.
Estimated effort
3–6 days.
Request to use
Implement Phase 1, starting with Phase 1A.

The subphases can be implemented separately.
Phase 2 — Make production authentication coherent
Objective: Ensure strict production mode supports the actual browser, API and public-share workflows.
Design decision
Choose and document one of these models:
Recommended: Session authentication with CSRF protection for the same-origin browser UI; bearer tokens for integrations and the SolidWorks add-in.
Bearer-token authentication for both browser and integrations, requiring a larger frontend token-management redesign.
The first model fits the existing application better.
Work
Define endpoint categories:Browser/session API.
Integration/bearer API.
Public share API.
Health/readiness endpoints.

Replace the global /api/* bearer requirement with explicit policy by endpoint category.
Preserve CSRF protection on session-authenticated state changes.
Add narrowly scoped public-share authorization instead of a broad public exemption.
Make authorization failures return consistent JSON errors.
Update frontend API handling to check response.ok.
Display authentication and authorization errors instead of empty datasets.
Add integration tests covering:Strict-mode browser login and normal navigation.
Browser CRUD requests.
Bearer-token requests.
Invalid and expired tokens.
Public shares.
CSRF rejection.
Permission and row-scope enforcement.

Make strict mode the default after all tests pass.
Acceptance criteria
The complete React UI works in strict mode.
Public shares work without exposing unrelated APIs.
API tokens remain unsuitable for browser-session substitution.
Strict mode becomes the documented and deployed default.
Compat mode is either removed or clearly marked as development-only.
Estimated effort
4–8 days.
Request to use
Implement Phase 2 using session authentication for the same-origin browser and bearer tokens for integrations.

Phase 3 — Repair deployment and supply-chain gates
Objective: Produce a reliable, vulnerability-gated fresh installation.
3A. Container initialization — [DONE 2026-08-03, Codex + parallel Caddy audit]
  DONE Replaced legacy entrypoint definitions with the canonical standard-role
       service and removed the missing PERMISSIONS import.
  DONE Bootstrap is idempotent for persisted guided-VPS seed settings; existing
       users, password hashes, assignments, local role drift and legacy roles are
       preserved.
  DONE Unrecoverable initialization failure prevents application launch.
  DONE Credentials originate in an operator helper/installer and are never
       generated or printed by the container.
  DONE Direct Compose, Windows one-folder, guided Caddy and standalone nginx
       first-admin workflows are documented and have safe migration behavior.
  DONE CI fresh-database Compose smoke verifies Mongo, exact canonical roles,
       administrator bootstrap, browser login, health, a protected bearer API,
       restart idempotence, log secrecy and false-success prevention.
  DONE Guided VPS/Caddy rendering, network isolation, update, rollback and doctor
       behavior are protected by six tests and local config validation evidence.
  CLOSED DEPLOY-SEED-01. Remaining deployment/infrastructure risks belong to
       Phase 5 and the live release-candidate exercise in Phase 10.

3B. Dependency remediation — [PARTIALLY DONE 2026-08-02, Claude, on main: 5a893e6 + f574515]
Done ahead of sequence while triaging the open Dependabot PRs. Landed on main, so
it is already in the base of this branch.
  DONE Upgraded runtime deps: requests 2.34.2, urllib3 2.7.0, idna 3.18,
       charset-normalizer 3.4.9, python-dotenv, psutil, tzdata.
  DONE Upgraded dev tooling: ruff 0.16.0, black 26.5.1, bandit 1.9.4,
       pytest-cov 7.1.0, pip-audit 2.10.1, cyclonedx-bom 7.3.0 (4.x->7.x major —
       verified the CI invocation still works), pre-commit 4.6.0.
  DONE Regression tested: clean venv, full suite 549 passed / 1 pre-existing
       failure, ruff and bandit clean.
  DONE Dependency-update automation already exists (Dependabot). Triaged all 17
       open PRs: 11 applied and closed, 5 left open with written rationale on each.
  PARTIAL "Keep runtime and development dependency updates in separate commits."
       NOT honoured — 5a893e6 mixes runtime and dev updates in one commit. Left as
       is since it is already pushed; worth splitting on any future batch.

  DONE Triaged every current Python and frontend advisory in 50d75e7. Detailed
       reachability, remedies and re-check triggers are in
       docs/security/dependency_advisory_triage.md.
  TODO Upgrade Pillow to 12.3.0, gunicorn to 22.0.0 and cryptography to 48.0.1
       as a bounded runtime-only commit. The gunicorn change must preserve and
       validate the guided VPS/Caddy path.
  TODO Migrate EOL PyPDF2 to pypdf in a separate commit. Twelve imports exist
       across docpacks.py, markup_documents.py and order_scope.py.
  TRIAGED React Router GHSA-qwww-vcr4-c8h2 affects unstable RSC APIs only; none
          are used. Core react-router 8.3.0 is patched, but react-router-dom has
          no 8.x. Do not accept npm audit fix --force's 7.11.0 downgrade because
          it restores five other advisories. Track the v8 migration in Phase 6.
  TRIAGED Flask-Security-Too GHSA-f66q-9rf6-8795 affects WebAuthn reauthentication
          only. TinyMRP does not enable WebAuthn or install its extras, and no
          patched release exists. Enabling WebAuthn makes this immediately
          applicable and blocking.
  PROPOSED EXC-2026-001 and EXC-2026-002 provide the formal exception records,
           compensating controls and voiding conditions. They are not accepted:
           a named human owner, sign-off date and expiry are still required.

Still-open Dependabot PRs and why (do not just merge these):
  #35 eslint 10  — blocked: eslint-plugin-react-hooks@5.2.0 peers on eslint <=9.
  #36 vite 8     — swaps the build engine to rolldown; needs a hands-on migration.
  #34 typescript 7 — no CI value: nothing runs tsc, and `tsc --noEmit` already
                     fails on the current 5.8 pin, so there is no green baseline.
  #43 python 3.14 — must move together with CI python-version and the py311
                    targets in pyproject.toml; reportlab already warns on 3.14.
  #28 node 26    — should move together with the node-version pin in security.yml.
3C. Reproducible supply chain — [BARELY STARTED]
  DONE (partial) GitHub Actions updated to current majors on main (5a893e6):
       metadata-action v6, build-push-action v7, login-action v4,
       setup-python v7, trivy-action 0.36.0. NOTE this is version pinning, NOT the
       SHA pinning this phase asks for — still a TODO below.
  NOTE SBOM generation already exists for release images and the backend
       (release-image.yml + security.yml both emit CycloneDX and upload it).
       "Generate an SBOM for every release" is therefore largely satisfied;
       verify it covers the frontend too.
  TODO Pin container base images by digest for release builds. Not started.
  TODO Pin GitHub Actions to immutable commit SHAs. Not started.
  TODO Store dependency audit reports as CI artifacts. Not started.
  TODO Review broad secret-scanner exclusions (.gitleaks.toml). Not started.
  TODO Make CI gates blocking. Currently non-blocking / advisory:
       - Black runs with continue-on-error (repo-wide reformat still pending:
         162 files would be reformatted at the current pin — see Phase 8).
       - shellcheck is marked informational in security.yml.
       These must not be flipped to blocking until Phase 8 burns the findings down.
Acceptance criteria
A clean machine can create a working deployment without manual database repair.
Initialization failure prevents a false-successful deployment.
High and critical dependency gates pass or have approved, time-limited exceptions.
Release images and workflows are reproducible.
A fresh-install smoke test runs in CI.
Estimated effort
4–8 days, depending on dependency compatibility.
Request to use
Completed. Do not repeat Phase 3A; preserve its bootstrap, Compose and Caddy
regression gates while implementing later phases.

Phase 4 — Protect imports and business data integrity
Objective: Prevent archive-based denial of service and partial imports.
4A. Upload limits
Enforce:Compressed request limit.
Per-entry uncompressed limit.
Total uncompressed limit.
Maximum entry count.
Maximum compression ratio.
Maximum filename/path length.
Allowed file-type policy.

Reject encrypted, malformed and ambiguous archives.
Stream large content rather than loading entire files into memory.
Use dedicated staging directories with bounded storage.
Ensure timeout and cleanup behavior is deterministic.
Add adversarial archive tests without committing dangerous large fixtures.
4B. Atomic import behavior
Separate import into:Parse and validate.
Prepare an execution plan.
Stage files.
Commit database and file changes.
Generate derived artifacts.

Use MongoDB transactions where the deployment topology supports them.
Otherwise implement a durable import journal and compensating rollback.
Give every import a unique operation ID.
Make retries idempotent.
Record created, changed, skipped and rolled-back objects.
Do not silently leave partial Parts/BOM records after filesystem failure.
Add fault-injection tests for:Disk failure.
Metadata failure.
Thumbnail failure.
Duplicate operations.
Process interruption.

Acceptance criteria
Oversized expansion is rejected before dangerous resource consumption.
Failed imports leave the system in the previous consistent state or a clearly recoverable journaled state.
Retrying an interrupted import does not duplicate data.
Operators can diagnose and reconcile every import operation.
Estimated effort
6–12 days.
Request to use
Implement Phase 4A, archive safety and resource limits.

Phase 5 — Production infrastructure and operational resilience
Objective: Make deployment secure, observable and recoverable.
Security defaults
Require MongoDB authentication.
Create a least-privilege application database user.
Remove weak and compatibility-mode defaults.
Fail startup when placeholder secrets are used.
Define an approved secret-delivery mechanism.
Move rate limiting to Redis or another shared store.
Decide explicitly whether rate-limit storage failure is fail-open or fail-closed by endpoint risk.
Apply global and endpoint-specific limits.
Health and monitoring
Create separate endpoints:
Liveness: process is responsive.
Readiness: MongoDB and required storage are usable.
Diagnostics: authenticated detailed status for operators.
Readiness should verify:
MongoDB query.
File-root existence and writeability where appropriate.
Free disk thresholds.
Required configuration.
Background-worker state once workers are introduced.
Add:
Structured JSON logging.
Request/correlation IDs.
Authentication and authorization failure metrics.
Import and document-pack duration/error metrics.
Disk and backup age alerts.
Central log retention guidance.
Alert thresholds and an operator runbook.
Backup and recovery
Encrypt backups.
Generate and verify checksums.
Send backups off-host.
Define retention tiers.
Exclude or securely handle plaintext secrets.
Test complete restoration of:MongoDB data.
Deliverables.
Configuration.
Users and roles.

Schedule automated restore verification.
Define commercial RPO and RTO targets.
Acceptance criteria
Default production Compose configuration uses authenticated MongoDB.
Rate limits are consistent across workers.
Readiness fails when critical dependencies fail.
Alerts exist for storage, database, backup and application failures.
A documented restore drill succeeds from an off-host backup.
Estimated effort
6–12 days.
Request to use
Implement Phase 5, beginning with authenticated MongoDB and readiness checks.

Phase 6 — Frontend reliability, usability and accessibility
Objective: Turn the polished interface into a testable and accessible commercial UI.
Testing foundation
Add Vitest and React Testing Library.
Add Playwright for critical browser workflows.
Add axe accessibility checks.
Cover:Login/logout.
Part search and detail.
Import preview and failure.
Job/order editing.
Role and permission behavior.
Token management.
Public shares.
Mobile/tablet layouts.

Usability
Add an application error boundary.
Add a real 404 page.
Standardize loading, empty, permission-denied and server-error states.
Replace console-only failures with actionable messages.
Avoid silently translating API failures into empty lists.
Reduce dense table friction with responsive column selection or card layouts.
Split the part-detail interface into smaller navigable sections.
Preserve unsaved-change warnings.
Review destructive-action confirmation patterns.
Accessibility
Add a skip link and stable main-content target.
Associate every label and input.
Add live regions for asynchronous success and error messages.
Verify keyboard-only operation.
Improve modal focus management.
Add reduced-motion support.
Validate color contrast.
Give icon-only controls accessible names.
Establish WCAG 2.2 AA as the target.
Performance
Introduce route-level lazy loading.
Lazy-load 3D and markup functionality only when needed.
Set bundle-size budgets in CI.
Measure real page performance with representative datasets.
Acceptance criteria
Critical workflows pass in Chromium, Firefox and WebKit.
Automated axe tests have no serious or critical violations.
Keyboard-only critical workflows are usable.
API failures produce clear user-facing states.
Bundle sizes meet defined budgets.
Estimated effort
7–15 days.
Request to use
Implement Phase 6, beginning with the frontend test and accessibility foundation.

Phase 7 — SolidWorks add-in commercial hardening
Objective: Make the desktop integration trustworthy and supportable.
Work
Move token storage to per-user protected storage.
Never fall back to plaintext silently.
Migrate existing plaintext configurations safely.
Avoid accepting secrets through command-line installer parameters.
Add server-certificate and backend URL validation.
Add token expiry and revocation UX.
Redact tokens from logs and diagnostic exports.
Align backend, add-in and installer versioning.
Obtain a commercial code-signing certificate.
Sign the add-in assemblies and installer.
Add installer upgrade/uninstall tests.
Test supported SolidWorks and Windows versions.
Define update and rollback behavior.
Acceptance criteria
Tokens are never stored in plaintext.
Multi-user machines do not share credentials.
Installer and binaries have valid trusted signatures.
Upgrade and uninstall preserve or securely remove configuration as documented.
Compatibility matrix and support boundaries are published.
Estimated effort
4–10 days, excluding certificate procurement.
Request to use
Implement Phase 7’s secure per-user token-storage migration.

Phase 8 — Maintainability and engineering controls
Objective: Reduce regression risk and make future commercial development sustainable.
Work
Enforce Black and the agreed Ruff rule set.
Expand mypy incrementally from the current two modules.
Remove unused imports, variables and redefinitions.
Review broad except Exception handlers.
Stop swallowing operationally significant failures.
Break up the largest backend services, views and frontend components.
Define service boundaries for:Parts/BOM.
Files.
Imports.
Jobs/orders.
Authorization.
Document generation.

Add a data/schema migration framework.
Add optimistic concurrency or version fields for collaborative edits.
Raise coverage thresholds progressively, with risk-weighted module targets.
Add API-contract tests.
Introduce CODEOWNERS and mandatory security review for sensitive areas.
Acceptance criteria
Formatting and lint gates are blocking.
Static typing covers security and business-critical services.
Critical mutation paths have concurrency protection.
Database changes have forward and rollback procedures.
Large modules have an agreed decomposition plan and measurable progress.
Estimated effort
Ongoing; initial gate hardening approximately 5–10 days.
Request to use
Implement Phase 8’s blocking formatting, linting and type-checking baseline.

Phase 9 — Product and commercial scope
Objective: Decide what the product is being sold as and close the corresponding functional gaps.
Track A: Engineering BOM/document-control product
This is the shorter route to market. Position features around:
Parts and revisions.
BOM control.
Engineering files.
Drawing review and markup.
Controlled document packs.
Jobs and purchasing/sales coordination.
SolidWorks integration.
Avoid claims of comprehensive production planning or inventory control.
Track B: Full MRP product
A separate product roadmap is needed for:
Stock and available-to-promise ledger.
Warehouses, bins and transfers.
Purchase receipts and material issues.
MRP explosion and demand netting.
Lead times, safety stock and reorder policies.
Routings and work centres.
Capacity planning.
Costing and variances.
Lot/serial traceability.
Cycle counting and stock adjustments.
Planning exception messages.
Accounting/export integrations.
Commercial documentation
Product terms/EULA.
Privacy policy and DPA.
Retention/deletion policy.
Support and SLA definitions.
Vulnerability disclosure contact.
Third-party software notices.
Trademark and bundled-asset provenance.
Customer onboarding/offboarding procedures.
Export and data portability commitments.
Accessibility statement.
Acceptance criteria
Marketing claims match implemented capabilities.
Every supported workflow has an owner and documented support boundary.
Third-party redistribution obligations are documented.
Customer contractual and privacy materials are approved by counsel.
Request to use
Help me execute Phase 9 for the engineering BOM/document-control market position.

Phase 10 — Release-candidate validation
Objective: Make the final commercial release decision using production-equivalent evidence.
Required validation
Freeze and tag a release candidate.
Deploy from scratch in a production-equivalent environment.
Run:Full automated suite.
Compose smoke test.
Browser matrix.
Accessibility audit.
Dependency and container scans.
Secret scan.
SAST.
External penetration test.
Load and endurance tests.
Large dataset/import tests.
Backup and complete restore drill.
Upgrade and rollback drill.
SolidWorks installer/upgrade tests.

Resolve findings or formally accept residual risks.
Produce:SBOM.
Release notes.
Deployment guide.
Operations runbook.
Support escalation process.
Signed artifacts.
Final go/no-go record.

Acceptance criteria
No unresolved critical or high findings without explicit, time-limited executive risk acceptance.
Load objectives, RPO and RTO are achieved.
Clean installation, upgrade, rollback and restore all succeed.
Release artifacts are signed and reproducible.
Production monitoring and escalation paths are active.
Estimated effort
1–3 weeks plus external penetration testing.
Request to use
Begin Phase 10 release-candidate validation and produce the final go/no-go report.

Recommended implementation order
The shortest safe path is:
Phase 0 — controlled baseline. DONE.
Phase 1A — stored XSS. DONE.
Phase 3A — fresh deployments. DONE 2026-08-03; Caddy/VPS path preserved.
Phase 1B — token lifecycle. DONE 2026-08-03.
Phase 1C — server-side session lifecycle. NEXT.
Phase 2 — strict-mode authentication.
Phase 3B/3C — dependencies and supply chain.
Phase 4 — safe, atomic imports.
Phase 5 — production infrastructure.
Phase 6 — frontend quality and accessibility.
Phase 7 — add-in hardening.
Phase 8 — maintainability.
Phase 9 — commercial scope.
Phase 10 — release validation.
The next implementation request should be: “Integrate the reviewed Phase 3B
advisory documentation, then implement Phase 1C server-side session lifecycle
and revocation while preserving the Phase 3A deployment and Caddy gates.”
